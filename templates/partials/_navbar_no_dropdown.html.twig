<nav class="navbar navbar-expand-lg navbar-light bg-light">
    
    <div class="container-fluid">
    
        <a href="{{ path('app_home')}}" class="d-flex align-items-center mb-2 mb-lg-0 text-dark text-decoration-none header-logo">
            <!-- logo Oh My Game-->
            <img src="{{ asset('images/ohmygame_logo.png') }}" height="40" alt="Logo">
            <span class="header-logo__title">Oh My Game</span>
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarSupportedContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_games_list')}}">Jeux</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_map')}}">Carte</a>
                </li>
                {% if is_granted("ROLE_USER") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_bibliogame')}}">Bibliogame</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_event_index')}}">Evénements</a>
                    </li>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_admin')}}">Administration</a>
                    </li>
                {% endif %}      
            </ul>
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if is_granted("ROLE_USER") %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profil')}}">{{ app.user.alias }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path("app_profil_chatting")}}">Messagerie</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout')}}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register')}}">S'inscrire</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login')}}">Connexion</a>
                    </li>
                {% endif %}
            </ul> 
        </div>
        
    </div>
</nav>