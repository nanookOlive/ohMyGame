{% extends 'admin/base.html.twig' %}

{% block title %}Editer {{ game.title }}{% endblock %}

{% block cardheader %}
    <h1>Editer {{ game.title }}</h1>
    <a class="btn btn-link" href="{{ path('app_admin_game_index') }}">Retour</a>
{% endblock %}
    
{% block cardbody %}
     {{ include('admin/game/_form.html.twig', {'button_label': 'Update'}) }}
{% endblock %}

{% block cardfooter %}
    <div class="text-end">
        {{ include('admin/game/_delete_form.html.twig') }}
    </div>
{% endblock %}